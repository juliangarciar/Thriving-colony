#ifndef RECAMERA_H
#define RECAMERA_H

#include <vector>
#include <algorithm>
#include <Types.h>
#include "TNode.h"
#include "TCamera.h"


class RECamera {
    public:
        /**
         * @brief 
         * 
         * @param sceneNode 
         */
        RECamera(TNode* sceneNode);

        /**
         * @brief 
         * 
         */
        ~RECamera();

    private:
        
};

#endif