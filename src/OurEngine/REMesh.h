#ifndef REMESH_H
#define REMESH_H

#include <vector>
#include <algorithm>
#include <Types.h>
#include "TNode.h"
#include "TMesh.h"


class REMesh {
    public:
        /**
         * @brief 
         * 
         * @param sceneNode 
         */
        REMesh(TNode* sceneNode);

        /**
         * @brief 
         * 
         */
        ~REMesh();

    private:
        
};

#endif